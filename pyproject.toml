[project]
name = "terracotta-create"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9.10",
    "terracotta==0.8.3",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
